apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.sparkjava', name: 'spark-core', version: '+'
}

task hello(type: JavaExec) {
    main = "lesson10.Hello"
    classpath = sourceSets.main.runtimeClasspath
}
