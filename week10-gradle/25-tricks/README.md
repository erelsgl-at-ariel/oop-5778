This folder illustrates two useful tricks:

* Creating an Eclipse project:  run gradle Eclipse
* Creating a Gradle wrapper: run gradle wrapper
* Creating a "deploy" task that depends on build and the wrapper
